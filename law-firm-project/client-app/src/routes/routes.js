/**
 * Entry định tuyến — re-export từ routes.jsx (Vite yêu cầu .jsx cho cú pháp JSX).
 * Import trong App: `import { appRoutes } from './routes/routes'`
 */
export { appRoutes } from './routes.jsx';
